pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)
    repositories {
        flatDir {
            dirs 'libs'
        }
        google()
        mavenCentral()
    }
}

rootProject.name = "Godot3.6AppsFlyerPlugin"
